import { EtherscanDirective } from './etherscan.directive';

describe('EtherscanDirective', () => {
  it('should create an instance', () => {
    const directive = new EtherscanDirective();
    expect(directive).toBeTruthy();
  });
});
