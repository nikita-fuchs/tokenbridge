import { ToEtherscanDirective } from './to-etherscan.directive';

describe('ToEtherscanDirective', () => {
  it('should create an instance', () => {
    const directive = new ToEtherscanDirective();
    expect(directive).toBeTruthy();
  });
});
